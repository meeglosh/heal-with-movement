{
  "dependencies": {
    "@neon/config": "^1.8.2",
    "@neon/env": "^1.4.7"
  },
  "name": "heal-with-movement-workspace",
  "private": true,
  "scripts": {
    "build": "npm --prefix site run build",
    "test": "npm --prefix site test",
    "dev": "npm --prefix site run dev"
  }
}
